# Claude Code kickoff — ENCAP, Phase 0 and Phase 1

Paste the block below as the first message in a Claude Code session opened in an empty `ENCAP/` directory that already contains `AGENTS.md` and `docs/PLAN.md` (this file can sit at `docs/KICKOFF.md`). Everything above the line is context for Kevin; everything below it is the prompt.

**Before starting the session:** create the empty repo (`git init`, or create it on GitHub under `semanticarchitectures` and clone), copy `AGENTS.md` to the root and `PLAN.md` to `docs/`, and commit those two files first so the session starts from a clean tree. Have the ENSIM repo location handy (`https://github.com/semanticarchitectures/ENSIM`, pin commit `aa7b714`). The doctrine PDFs are not needed to start — they gate Phase 1's exit, and the prompt tells Claude Code to stop and ask when it reaches that point.

---

This repo is ENCAP. Read `AGENTS.md` first, then `docs/PLAN.md` in full. Both are decided documents, not proposals: the plan's Sections 3 and 4 were accepted on 2026-09-15, and `AGENTS.md` Section 1 lists what is not open for re-litigation. If anything you're about to do conflicts with either file, stop and flag it.

Also read, directly from source rather than from the plan's summary of them:

- The OKF spec: https://github.com/GoogleCloudPlatform/knowledge-catalog/blob/main/okf/SPEC.md — and look at the `samples/` directory in that repo, which Phase 1's tests will use.
- ENSIM's `AGENTS.md` and `docs/architecture/ARCHITECTURE.md` at https://github.com/semanticarchitectures/ENSIM, commit `aa7b714`. `AGENTS.md` here is adapted from ENSIM's; the architecture doc's Section 6 is the data model the operating-model profile will have to represent, and Section 12 is the sibling-project pattern this repo follows with ENSIM.

Then do Phase 0 and Phase 1 from `docs/PLAN.md` Section 5, in order, committing at each step with a message that says what was done and what was verified (not just what changed).

**Phase 0 — scaffold.** npm workspaces (`packages/*`, `apps/*`, `eval/*`), scope `@encap/*`, TypeScript, Node 20+, a root `Justfile` with `build`, `test`, `validate`, and `fetch-ensim` recipes. `LICENSE` (Apache 2.0) and `NOTICE` at the root. Empty package directories for everything in the plan's Section 6 tree, each with a `README.md` stating its one-paragraph purpose from the plan and a `package.json` — no code yet in packages other than `okf-format`. `fixtures/doctrine/` with a `manifest.schema.json` (source URL, sha256, publication date, distribution statement, and a `note` field) and an empty `manifest.json` that validates. `fixtures/competency-questions/` with a schema for a question set and one example file. `eval/ensim-harness/` with the `fetch-ensim` script: clone ENSIM at a pinned SHA (default `aa7b714`, overridable), copy `packages/org-doctrine-model/data/` and `schema/` into `fixtures/ensim@<sha>/`, write a `provenance.json` (SHA, date fetched, ENSIM's license), and honor an `ENSIM_PATH` override. Run it once and commit the snapshot. Phase 0 gate: `just validate` passes with the empty manifest and the ENSIM snapshot validating against ENSIM's own schemas.

**Phase 1 — `packages/okf-format`.** Read, validate, and write OKF v0.2 bundles: frontmatter schema (required `type`; recommended `title`, `description`, `resource`, `tags`; the provenance, trust, and lifecycle families; `okf_version` on the root `index.md`), a permissive read schema that preserves unknown keys and a strict emit schema; link extraction into a directed edge list (absolute bundle-relative and relative forms, broken links tolerated and reported, never rejected); footnote-to-`sources[].id` resolution; `index.md` and `log.md` handling; the custom `relations:` key (schema in this package, semantics in `operating-model` later). A strict-provenance validation mode per `AGENTS.md` Section 4. An ingest step with the process-boundary contract from the plan's Section 3.4: input a file path, output plain Markdown with page anchors; pick one extractor for the first implementation, keep the interface extractor-agnostic, and document how to swap it. Phase 1 gate, two parts: (a) every bundle in Google's `samples/` round-trips through read → write byte-for-byte on content and structurally on frontmatter, and its link graph and footnote resolution are covered by tests; (b) a doctrine PDF from `fixtures/doctrine/` becomes Markdown with page anchors good enough to cite. Part (b) needs the PDFs Kevin is downloading. If `fixtures/doctrine/manifest.json` is still empty when you reach it, stop, say Phase 1 is complete except gate (b), and ask — do not substitute a different document to make the gate pass.

Ask before deciding anything the plan leaves open. Do not begin Phase 2.
