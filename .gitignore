node_modules/
dist/
build/
*.tsbuildinfo
.DS_Store
npm-debug.log*

# Doctrine PDFs are downloaded locally per fixtures/doctrine/manifest.json;
# only the manifest and derived Markdown are committed.
fixtures/doctrine/*.pdf

# Non-pinned local override for eval/ensim-harness (see its README).
.ensim-local/

# Vendored docling install for the okf-format ingest boundary (see
# packages/okf-format/scripts/setup-docling.sh) — Python, never committed,
# same contained-dependency pattern as ENSIM's vendor/ for Portico.
.venv-docling/
