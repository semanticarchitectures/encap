{
  "name": "@encap/eval-runner",
  "version": "0.1.0",
  "private": true,
  "description": "Competency-question runner shared by every phase gate from Phase 2 onward.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate": "npm run build && vitest run",
    "run-fixture-gate": "node scripts/run-fixture-gate.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.126.0",
    "@encap/okf-format": "*",
    "@encap/synthesis-agent": "*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "vitest": "^4.1.11"
  }
}
